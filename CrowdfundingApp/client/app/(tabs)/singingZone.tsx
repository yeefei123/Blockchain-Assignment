
import { View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SingingZone = () => {
    return(
        <SafeAreaView>
            <View>
                
            </View>
        </SafeAreaView>
    )
}

export default SingingZone;